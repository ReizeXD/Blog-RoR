<div class="post-details">
    <h2><%= post.title %></h2>
    <p><%= post.content %></p>
    <small>Publicado por: <%= post.user.name %> em <%= post.created_at.strftime('%d/%m/%Y') %></small>
  </div>
  